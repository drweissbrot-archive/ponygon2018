.panel {
	&:not(:last-child) {
		margin-bottom 2rem
	}

	&.--no-title {
		.body {
			border-radius .5rem
		}
	}

	.title {
		padding 2rem 4rem
		background #555

		border-radius .5rem .5rem 0 0
	}

	.body {
		padding 2rem 4rem
		background #333

		border-radius 0 0 .5rem .5rem
	}
}
